Decay time of the drum model (Plaits' MORPH). At maximum the 808 kick rings for several seconds.

Shares its underlying parameter with the oscillator's wave-index, so anything patched or automated to wave-index modulates Decay.
